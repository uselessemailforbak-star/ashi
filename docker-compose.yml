version: '3.8'

services:
  web:
    image: nginx:latest
    container_name: im-sorry-app
    ports:
      - "80:80"
    volumes:
      - .:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    environment:
      - TZ=UTC
    restart: unless-stopped
